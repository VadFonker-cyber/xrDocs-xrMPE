{
  "name": "xr-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --open /",
    "optimize:assets": "node scripts/optimize-assets.mjs",
    "prepare:content": "node scripts/generate-content-data.mjs",
    "build": "npm run prepare:content && tsc && vite build && node scripts/prerender.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.2.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "sharp": "^0.34.5",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "allowScripts": {
    "sharp@0.34.5": true
  }
}
