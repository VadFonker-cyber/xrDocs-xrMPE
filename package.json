{
  "name": "xr-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --open /",
    "optimize:assets": "node scripts/optimize-assets.mjs",
    "build": "npm run optimize:assets && tsc && vite build && node scripts/prerender.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "sharp": "^0.34.5",
    "typescript": "^5.8.3",
    "vite": "^7.3.1"
  }
}
