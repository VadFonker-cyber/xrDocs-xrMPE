{
  "name": "xr-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "npm run prepare:content && vite --host 127.0.0.1 --open /",
    "optimize:assets": "node scripts/optimize-assets.mjs",
    "prepare:content": "node scripts/generate-content-data.mjs",
    "build": "npm run optimize:assets && npm run prepare:content && tsc && vite build && node scripts/prerender.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.2",
    "sharp": "^0.34.5",
    "typescript": "^5.8.3",
    "vite": "^8.0.14"
  }
}
